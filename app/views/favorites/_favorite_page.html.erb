テスト
<#--% if current_user.likes.include?(micropost) % -->
  <# --%= form_with(model: current_user.favorites.find_by(micropost), local: true, method: :delete) do |f| %>
    <#-- %= hidden_field_tag :micropost_id, user.id %>
    <#-- %= f.submitn 'Unfavorite', class: 'btn btn-danger btn-block' %>
  <#-- % end %>

<#--% else %>
  <#--%= form_with(model: current_user.favorites.build, local: true) do |f| %>
  <#-- %= f.submit 'Favorite', class: 'btn btn-primary btn-block' %>
  <#--% end %>
<# --% end % -->
